## 🔴 Hardcoded Password [CRITICAL]

**Location:** `main.py:200`
**Priority Score:** 100
**Effort to Fix:** MINUTES

### Problem
Password is hardcoded in source code, exposing credentials in version control.

### Fix
Move credentials to environment variables and use python-dotenv for local development.

### Steps
1. IMMEDIATELY rotate the exposed credential — assume it is compromised
2. Create a `.env` file (add to .gitignore if not already)
3. Move the credential to the `.env` file: `DB_PASSWORD=your_password`
4. Install python-dotenv: `pip install python-dotenv`
5. Load env vars with `load_dotenv()` at app startup
6. Replace hardcoded value with `os.environ.get('DB_PASSWORD')`
7. Add startup validation to fail fast if required env vars are missing
8. For production, use a secrets manager (AWS Secrets Manager, Vault, etc.)

### Testing
Run `git log -p | grep -i password` to check if credential was ever committed to history

*Generated by [CodeDebt Guardian](https://github.com/Priyanshjain10/codedebt-guardian) 🤖*
